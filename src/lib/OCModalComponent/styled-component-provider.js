import styled from 'styled-components';
import { closeButtonStyle, mainComponentStyle, modalStyle } from './css-provider';

export const ContentModal = styled.div`
  ${modalStyle}
`;

export const StyledOCModalComponent = styled.section`
  ${mainComponentStyle}
`;

export const CloseButton = styled.a`
  ${closeButtonStyle}
`;
